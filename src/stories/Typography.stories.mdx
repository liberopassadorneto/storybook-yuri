import { Meta, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Style Guide/Typography" />

export const typography = {
  type: {
    primary: '"Montserrat", sans-serif',
  },
  weight: {
    regular: '400',
    medium: '500',
    semibold: '600',
    bold: '700',
  },
  size: {
    xs: 9,
    sm: 12,
    md: 16,
    lg: 18,
    xl: 22,
    xl2: 36,
  },
};

export const SampleText = 'Mais agilidade, menos custos';

# Typography

**Font:** Montserrat

**Weights:** 400(regular), 500(medium), 600(semibold), 700(bold)

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.md),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size.xl2),
  ]}
  fontWeight={typography.weight.semibold}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>
